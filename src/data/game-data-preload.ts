/**
 * Content ids for BootScene JSON preload. Keep in sync with public/data/game.json.
 */
export const gameDataPreload = {
  movementId: 'player',
  levels: ['level1'],
  collectibles: ['coin'],
  enemies: ['patrol-slime'],
  animations: ['player'],
} as const;
