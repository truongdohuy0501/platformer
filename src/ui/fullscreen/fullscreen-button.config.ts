export const FullscreenButtonConfig = {
  marginPx: 12,
  size: 44,
  fillColor: 0xffffff,
  idleAlpha: 0.22,
  pressedAlpha: 0.38,
  strokeColor: 0xffffff,
  strokeWidth: 2,
  /** Phaser key code for toggle (default: F). */
  keyboardKey: 'F',
} as const;
